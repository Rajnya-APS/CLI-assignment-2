# Question 3

## Aim

To implement an employee database using Linux system calls.

## Description

The program creates an employee database using the open(), write(), read(), lseek(), and close() system calls. It stores employee records in a file, updates the salary of an existing employee, and reads all records to verify the changes.
